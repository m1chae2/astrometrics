"""Purpose: In-process hardware simulators for wayfindinglib drivers.

Description: Not test files -- these are real, importable driver
implementations (e.g. `SimulatorIndiInterface`) used both by the test
suite and by production simulator-mode configuration
(`wayfindinglib.drivers.SimulatorIndiInterface`). Kept separate from
`drivers/test/`, which holds actual pytest test cases.
"""
