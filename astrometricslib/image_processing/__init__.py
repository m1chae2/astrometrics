"""Tools for working with raw image pixels.

This folder holds the basic building blocks every other part of the
library uses to read a photo's pixels correctly, find stars in it, and
check things like brightness. Nothing in here needs to know about
telescopes, stacking, or any of the higher-level science steps -- it
only deals with the pixels themselves.
"""
