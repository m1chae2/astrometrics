# Targets to skip during a full-catalog run.
#
# Usage:
#   run_all_target_processing.py --skip-targets-from astrometricslib/scripts/skip_targets.txt
#
# Blank lines and lines starting with # are ignored.
#
# Only targets that star-based registration can NEVER handle belong in
# the active list below. Siril aligns frames by matching star fields, so
# a target that presents a bright disk (like the Sun or Jupiter) and no
# surrounding stars has nothing to register against, no matter how many
# frames are taken.

Sun
Venus
Jupiter

# ---------------------------------------------------------------------
# NOT skipped, deliberately: data-limited, not impossible.
#
#
# The targets below failed previously, but only because they didn't have
# enough valid frames yet. Skipping them would hide targets that will
# start working the moment more data is captured, so they stay in the run
# and are expected to fail until they have enough data.
#
#   Polaris   1 frame          registration needs at least two frames
#   M 44      1 frame          same
#   M 57      1 frame          same
#   Mirfak    3 frames         spectral stack found no valid output
#   Capella   5 frames         only 2 stars in reference; sparse field
#   Arcturus  61 frames        mixed target, only 1 standard frame
#                              remained after the standard/spectral split
#
# M 42 is also expected to stack successfully now that we handle sky
# brightness changes better (like filtering out frames with too much
# moonlight or twilight), so it must not be skipped.
